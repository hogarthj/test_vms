---
dependencies:
    - { role: 'httpd', when: inventory_hostname in groups.httpd }
    - { role: 'nginx', when: inventory_hostname in groups.nginx }
    - { role: 'mysql-server', when: inventory_hostname in groups.mysql }
    - { role: 'postgres-server', when: inventory_hostname in groups.postgres }
...
